# 2026-09-04｜公共上下文机制冻结审计

## 参考来源

参考 ProFlow `spec/平台架构与公共约定/00-公共上下文` 的成熟治理经验，但未复制其 Deployment/Real3/Browser 等 Phase 3 专用知识。

## 迁移的核心机制

- 四层 ownership：长期规则 / CURRENT / Runbook / 历史。
- 新 Chat 固定最小读取顺序。
- `CURRENT.REQUIRED_CONTEXT` 按当前阶段动态路由。
- Spec / Git / Runtime / 实战档案的真源分层。
- Round Closeout 写回算法。
- `DO_NOT_REPEAT` 防止换 Chat 历史倒退。
- 失忆接管检查作为上下文治理 Gate。

## RAG 项目定制

新增“实现过程即学习过程”和“实战档案是阶段闭环产物”；CURRENT 直接以 P0→P8 为阶段轴；执行知识库只创建当前 P0 流程，后续按需增长，不把公共上下文提前做成百科全书。

## 审计结论

`PASS`：入口、当前状态、Authority、Required Context、Next Action、Do Not Repeat、Closeout 和历史边界均有唯一 owner。下一 Gate 为 P0 工程骨架。